/*

29.- The “advanced search” feature of many search engines allows you to use Boolean operators for complex queries, such as “(cats OR dogs) AND NOT pets”. Contrast these search operators with the Boolean operators in Java.

A.
	That java boolean operators are using boolean logic to generate a final unique boolean value based on previews values. In the case of search engines the operators are not generating a new boolean value but instead filtering the list of results based on a series of boolean values that the operators change.

*/

